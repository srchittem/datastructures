
#include <stdio.h>
#include <math.h>
void find_repeating_numbers(int *arr,int size)
{
	int i =0;
	for (i=0;i<size;i++)
	{
		if(arr(abs(arr[i])) > 0)
		{
			
		}
	}
}




void  main()
{

	int arr[] = {1,2,3,2,4,4};
	find_repeating_numbers(arr,sizeof(arr)/sizeof(arr[0]));
}
